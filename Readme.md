# map

  Map utility

## Installation

    $ component install jb55/map

## API

### map(array, fn)

  Map returning a new array:

```js
var names = map(users, function(user){
  return user.name;
});
```

# License

  MIT
